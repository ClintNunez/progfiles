#if !defined(CUSTOM_BINARY_CONVERTER_H)
#define CUSTOM_BINARY_CONVERTER_H

namespace custom_binary_converter {
    // use the method of dividing the decimal number with 2 and getting the remainder
}

#endif
