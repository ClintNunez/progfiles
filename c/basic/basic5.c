#include <stdio.h>

int main() {
    const int HEIGHT = 7;
    const int WIDTH = 5;
    
    printf("Perimeter of the rectangle = %i\n", ((HEIGHT + WIDTH) * 2));
    printf("Area of the rectangle = %i", (HEIGHT * WIDTH));

    return 0;
}
