#include <stdio.h>

int main() {
    int ctr = 0;
    float amount, currencyArr[] = {100, 50, 20, 10, 5, 1, 0.50, 0.25};

    printf("Input the amount: ");
    scanf("%f", &amount);

    while(amount != 0) {

    }

    return 0;
}
