#if !defined(BINARY_SEARCH_H)
#define BINARY_SEARCH_H

#include <vector>

namespace binary_search {
    inline size_t find(std::vector<int> data, int element) {
        size_t n;

        return n;
    }
}

#endif
