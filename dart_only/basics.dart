/*
### TYPES
Search for:
    /T_ + Number || Strings || Booleans || Lists || Sets || Maps || Runes || Symbols
    

*/

void main() {
    


}

void T_Numbers() {
    var x = 1;  // int x = 1;
    var hex = 0x32; // int hex = 0x32;

    var y = 1.1; // double y = 1.1;
    var exponent = 1.1e5; // double expnonet = 1.1e5;

    double z = 1; // Equivalent to double z = 1.0; "Integer literals are automatically converted to doubles when necessary" TODO test for arithmetic operations

    // variables declared as num can have both integer and double values
    num a = 1; 
    a += 1.1e5;
}
